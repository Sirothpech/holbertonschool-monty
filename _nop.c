#include "monty.h"

/**
 * _nop - function doesn’t do anything.
 * @stack: pointer to the stack
 * @line_number: line number of the file being read
 */
void _nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
